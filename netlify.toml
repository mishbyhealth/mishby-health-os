# netlify.toml — v14 routing for /api/plan to Netlify Functions
[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

[[redirects]]
  from = "/api/plan"
  to = "/.netlify/functions/plan"
  status = 200
